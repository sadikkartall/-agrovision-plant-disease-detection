[phases.setup]
nixPkgs = ["git", "git-lfs"]

[phases.install]
cmds = [
  "git lfs install",
  "git lfs pull || true",
  "pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "echo 'Build completed'"
]

